<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head><meta charset="UTF-8"/><title>Login</title></head>
<body>
  <div th:replace="~{layout :: header}"></div>
  <main>
    <h1>Login</h1>
    <div class="card">
      <div th:if="${loginError}" class="error" th:text="${loginError}"></div>
      <form class="stack" th:action="@{/login}" method="post" th:object="${loginForm}">
        <div class="row"><label for="username">Username</label><input id="username" type="text" th:field="*{username}" /></div>
        <div class="row"><label for="password">Password</label><input id="password" type="password" th:field="*{password}" /></div>
        <div><button type="submit">Sign in</button> <a th:href="@{/register}">Create an account</a></div>
      </form>
    </div>
  </main>
  <div th:replace="~{layout :: footer}"></div>
</body>
</html>